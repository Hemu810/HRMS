# Root .env.example — see per-folder templates for full variable lists:
#   backend/.env.example   — DB, SMTP, server, seed passwords
#   frontend/.env.example  — VITE_API_URL, VITE_PAYSLIP_EMAIL_ENDPOINT
